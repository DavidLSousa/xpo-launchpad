export const BorderRadius = {
  /** 4 — inputs */
  sm: 4,
  /** 8 — cards secundários */
  md: 8,
  /** 12 — cards e modais (padrão) */
  lg: 12,
  /** 30 — botão primário (pill) */
  full: 30,
  /** 50 — avatares circulares */
  circle: 50,
} as const;

export const BorderWidth = {
  /** 0.4 */
  xs: 0.4,
  /** 1 */
  sm: 1,
  /** 2 */
  md: 2,
};
